#!/bin/bash
# 卸载后兜底：确保没有残留进程占端口（应用目录可能已被移除，走固定标记路径）。
APP="zcode"
for pid in $(pgrep -f "/@appcenter/${APP}/" 2>/dev/null; pgrep -f "/var/apps/${APP}/" 2>/dev/null); do
    kill -KILL "${pid}" 2>/dev/null || true
done
exit 0
